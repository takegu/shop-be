export type ProductWithCount = {
  description: string,
  price: number,
  title: string,
  count: number,
};

export type Product = {
  description: string,
  product_id: string,
  price: number,
  title: string,
};

export type Stock = {
  id: string,
  count: number,
};
